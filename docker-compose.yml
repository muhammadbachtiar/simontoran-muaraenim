services:
  ci3:
    build: .
    container_name: ci3-apache
    environment:
      - TZ=Asia/Jakarta
    ports:
      - "8083:80"
    restart: always
    networks:
      - simontoran-network
    depends_on:
      - db
    volumes:
      - simontoran-uploads:/var/www/html/uploads

  db:
    build:
      context: .
      dockerfile: Dockerfile.db
    container_name: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: Kominfo@1234!@
      MYSQL_DATABASE: simontoran_muaraenim
      MYSQL_USER: simontoran_user
      MYSQL_PASSWORD: Kominfo@1234!@
    volumes:
      - simontoran-db-data:/var/lib/mysql
    restart: always
    networks:
      - simontoran-network

volumes:
  simontoran-db-data:
  simontoran-uploads:


networks:
  simontoran-network:
    driver: bridge
